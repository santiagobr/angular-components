import { TransformVowelsPipe } from './transform-vowels.pipe';

describe('TransformVowelsPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformVowelsPipe();
    expect(pipe).toBeTruthy();
  });
});
